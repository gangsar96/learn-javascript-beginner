const schedule = ["wake up","eat","film a video","watch on the youtube"];

// console.log(schedule);
//schedule.shift();// remove a beginning
//schedule.unshift("hey you"); // add in the beggining
//schedule.pop(); //removing on a last array
schedule.push('Hey a new thing got added');

console.log(schedule);

const film = schedule.indexOf("film a video");
console.log(schedule[film]);

// console.log(schedule.indexOf("eat"));